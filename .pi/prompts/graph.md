---
description: Generate or update a dependency graph from a report
argument-hint: "<report-path-or-slug>"
---

Use the `dependency-graph-writer` agent to create or update the dependency
graph for:

$ARGUMENTS

The agent and its skill own the procedure. Pull latest `master` before editing,
and open a draft PR when the graph is ready.
